var bomb;
var path;
var power;
var runner;
function preload(){
  //pre-load images
bomb.addAnimation=("bomb");
path.addAnimation=("path")
}

function setup(){
  createCanvas(400,400);
  //create sprites here
bomb=createSprite=(300,300,10,10);
path=createSprite=(200,200,20,20);
power=createSprite=(100,100,30,30);
}

function draw() {
  background(0);

}
